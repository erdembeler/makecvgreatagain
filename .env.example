# Optional. No API key is required. The application does not call any cloud service.
# Run `ollama pull llama3.2` if you want local AI extraction.
OLLAMA_MODEL=llama3.2
OLLAMA_BASE_URL=http://127.0.0.1:11434

# Optional: change these if another local application uses the default ports.
# CV_WEB_PORT=5173
# CV_API_PORT=3001
